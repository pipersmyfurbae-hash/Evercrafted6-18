# Migration Ledger

This ledger records reviewed schema changes, generated Drizzle artifacts, managed-database application evidence, and rollback considerations for the shared Ever Engine.

| Migration | Date | Scope | Review and application evidence | Rollback consideration |
|---|---|---|---|---|
| `0000_flawless_ezekiel` | 2026-08-17 | Canonical Ever Engine baseline | Applied prior to the current increment; provides the multi-tenant workspace, Studio, asset, job, entitlement, audit, and lead foundation. | Baseline is not rolled back independently. Restore a prior checkpoint only after data-impact assessment. |
| `0001_romantic_rocket_raccoon` | 2026-08-19 | Persistent notification delivery preferences | Drizzle generated the versioned artifact. The reviewed incremental SQL creates only `notificationPreferences` with a unique user key and foreign key to `users`; it was applied successfully through the managed database migration execution path. Runtime notification helpers gate assigned-review, project-stage, and provider-handoff job delivery through the persisted in-app preference. | Drop `notificationPreferences` only after confirming no preference data is needed; notification records remain unaffected. |
| `0002_deep_the_phantom` | 2026-08-19 | Provider-neutral workspace subscription lifecycle and period-scoped usage ledger | Drizzle generated the reviewed additive artifact. The migration creates `workspaceSubscriptions` (11 columns, plan/workspace foreign keys, lifecycle enum, provider identifier uniqueness) and `workspaceUsage` (8 columns, workspace foreign key, metric-period uniqueness) plus supporting indexes. It was applied successfully through the managed database path; an information-schema verification returned both tables with their expected column counts. | Drop only after assessing active subscription-history and usage-reporting needs. The application does not store payment credentials or invoke a billing provider. |
| `0003_messy_gambit` | 2026-08-19 | Persisted non-secret Personal integration controls | Drizzle generated and the review confirmed an additive `platformIntegrationControls` table only: integration key, readiness status, enablement intent, review note, reviewing owner, review timestamps, and a unique integration key. It was applied through the managed database path; information-schema verification returned 9 columns. No credentials, tokens, provider payloads, or payment data are stored. | Drop only after assessing platform operational history needs. Recreating this state never activates an external provider or reconstructs a credential. |
| `0004_certain_blonde_phantom` | 2026-08-19 | Checkpoint A Guided Wreath Creation source, interpretation, approval, consent, stage, and provenance foundation | Drizzle generated the reviewed additive artifact. It creates `memoryEntries`, `essenceProfiles`, `memoryStories`, `guidedStageStates`, `stageApprovals`, `memoryThreadEvents`, and `memoryConsents`, with foreign keys to existing projects/users, project-version or project-consent uniqueness, and project/status/timeline indexes. The migration was applied successfully through the managed database path after review; it does not alter or delete existing tenant, project, asset, Studio, or commercial data. | Drop only after assessing retained private-memory, consent, approval, and provenance records. A rollback must not destroy customer source material or leave a project with untraceable guided-state references. |
| `0005_amusing_tarot` | 2026-08-19 | Checkpoint B Guided Florals reference catalog, role sets, candidates, Wreath Tray selections, and compatibility reports | Drizzle generated the reviewed additive artifact. It creates `botanicalReferenceCatalog`, `guidedFloralRoleSets`, `guidedFloralCandidates`, `guidedWreathTraySelections`, and `guidedFloralCompatibilityReports`, with foreign keys to existing Guided Wreath/project/user records and unique project/role or role/rank constraints. The initial apply stopped after the first two new tables because an auto-generated foreign-key name exceeded the database identifier limit. No existing table was altered or deleted. The migration artifact was corrected only to shorten foreign-key identifiers; the safely confirmed partial state was resumed with the remaining tables, constraints, and indexes. | Drop only after confirming customer role choices, provenance, and compatibility history are no longer required. A rollback must preserve the relationship between saved tray choices and their original role/candidate snapshot. |
| `0006_secret_ironclad` | 2026-08-19 | Checkpoint C immutable Recipe snapshot, Recipe items, and simplified Blueprint foundation | Drizzle generated the reviewed additive artifact. It creates `guidedWreathRecipes`, `guidedWreathRecipeItems`, and `guidedWreathBlueprints`, with project/role-set/selection/catalog/user foreign keys, version constraints, status indexes, and no existing-table alteration. Pre-application identifier inspection found one generated foreign-key name over the database limit; it was shortened in the generated SQL before the first execution. The reviewed migration then applied successfully in one additive execution. | Drop only after assessing retained Recipe/Blueprint history. A rollback must not leave later stale records, stage approvals, Memory Thread provenance, or audit evidence pointing to removed source versions. |
| `0007_acoustic_black_crow` | 2026-08-19 | Checkpoint D versioned render-package provenance and manual provider-neutral handoff intent | Drizzle generated the reviewed additive artifact. It creates `guidedRenderPackages` and `guidedManualRenderHandoffs` with source Recipe/Blueprint, version/status, manifest, approval/stale, request-note, user, project, uniqueness, and status-index controls. One generated handoff-to-package foreign-key identifier was shortened to `guided_render_handoff_package_fk` before the first execution to meet the database identifier limit. The reviewed additive SQL then applied successfully through the managed database path; no existing table was altered, deleted, or recreated. | Drop only after confirming retained render-package provenance, approval decisions, stale explanations, and handoff intent are no longer required. A rollback must not orphan Memory Thread, audit, stage-approval, Recipe, or Blueprint history. |

> The initially generated `0001` file reproduced the historical baseline and was **not** applied. It was corrected to the reviewed incremental table creation above before execution, preventing duplicate-table operations against the active database.
