import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ArrowRight, CheckCircle2, FileUp, Layers3, PackageCheck, ScanSearch, Send, ThumbsDown } from "lucide-react";
import { ChangeEvent, useEffect, useMemo, useState } from "react";

const stageOrder = ["draft", "active", "in_review", "approved", "delivered"] as const;

function readAsBase64(file: File) {
  return new Promise<string>((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = () => reject(new Error("The selected file could not be read"));
    reader.onload = () => resolve(String(reader.result).split(",")[1] ?? "");
    reader.readAsDataURL(file);
  });
}

export default function Studio() {
  const { isAuthenticated } = useAuth();
  const spaces = trpc.workspace.listMine.useQuery(undefined, { enabled: isAuthenticated });
  const [workspaceId, setWorkspaceId] = useState<number | undefined>();
  const [projectId, setProjectId] = useState<number | undefined>();
  const [uploadError, setUploadError] = useState<string | null>(null);
  const [reviewNote, setReviewNote] = useState("");
  const [reviewerUserId, setReviewerUserId] = useState<number | undefined>();
  const [destinationType, setDestinationType] = useState("secure_link");
  const [destinationRef, setDestinationRef] = useState("");
  useEffect(() => { if (!workspaceId && spaces.data?.[0]) setWorkspaceId(spaces.data[0].id); }, [spaces.data, workspaceId]);
  const projects = trpc.project.list.useQuery({ workspaceId: workspaceId ?? 0 }, { enabled: Boolean(workspaceId) });
  useEffect(() => { if (!projectId && projects.data?.[0]) setProjectId(projects.data[0].id); }, [projects.data, projectId]);
  const members = trpc.workspace.listMembers.useQuery({ workspaceId: workspaceId ?? 0 }, { enabled: Boolean(workspaceId) });
  const assets = trpc.asset.list.useQuery({ workspaceId: workspaceId ?? 0, projectId }, { enabled: Boolean(workspaceId) });
  const reviews = trpc.studio.listReviews.useQuery({ workspaceId: workspaceId ?? 0, projectId: projectId ?? 0 }, { enabled: Boolean(workspaceId && projectId) });
  const deliveries = trpc.studio.listDeliveries.useQuery({ workspaceId: workspaceId ?? 0, projectId: projectId ?? 0 }, { enabled: Boolean(workspaceId && projectId) });
  const transition = trpc.studio.transitionProject.useMutation({ onSuccess: () => projects.refetch() });
  const upload = trpc.asset.uploadBase64.useMutation({ onSuccess: () => { setUploadError(null); assets.refetch(); } });
  const downloadUrl = trpc.asset.downloadUrl.useMutation({ onSuccess: result => window.open(result.url, "_blank", "noopener,noreferrer") });
  const requestReview = trpc.studio.requestReview.useMutation({ onSuccess: () => { setReviewNote(""); reviews.refetch(); } });
  const respond = trpc.studio.respondToReview.useMutation({ onSuccess: () => reviews.refetch() });
  const createDelivery = trpc.studio.createDelivery.useMutation({ onSuccess: () => { setDestinationRef(""); deliveries.refetch(); } });
  const readyDelivery = trpc.studio.markDeliveryReady.useMutation({ onSuccess: () => deliveries.refetch() });
  const selectedProject = useMemo(() => projects.data?.find(project => project.id === projectId), [projects.data, projectId]);

  const onFile = async (event: ChangeEvent<HTMLInputElement>) => {
    const file = event.target.files?.[0];
    if (!file || !workspaceId) return;
    if (file.size > 5 * 1024 * 1024) { setUploadError("Choose a file smaller than 5 MiB for this initial governed upload flow."); return; }
    try {
      const base64 = await readAsBase64(file);
      upload.mutate({ workspaceId, projectId, name: file.name, mediaType: file.type || "application/octet-stream", base64 });
    } catch (error) { setUploadError(error instanceof Error ? error.message : "The file could not be prepared for upload."); }
  };

  return <DashboardLayout><div className="mx-auto max-w-6xl space-y-7">
    <section className="overflow-hidden rounded-[2rem] bg-[#516f5e] p-7 text-[#f7f5ef] sm:p-10"><p className="text-xs font-semibold uppercase tracking-[0.15em] text-[#f3d5aa]">Moodoor Studio</p><div className="mt-4 grid gap-7 lg:grid-cols-[1.15fr_0.85fr]"><div><h1 className="text-3xl font-semibold tracking-[-0.05em] sm:text-4xl">The creative room is part of the operating system.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-[#e8eee9]/80">Studio work uses the same workspace, membership, projects, audit events, review decisions, delivery records, and asset policy as the rest of Evercrafted.</p></div><div className="rounded-3xl border border-white/15 bg-[#17202b]/25 p-5"><p className="text-sm font-medium">Controlled delivery path</p><p className="mt-2 text-sm leading-6 text-[#e8eee9]/75">External publishing remains disconnected until a reviewed provider adapter is configured. Delivery preparation is nevertheless traceable now.</p></div></div></section>

    <Card className="border-border bg-white/80"><CardContent className="grid gap-4 p-5 md:grid-cols-2"><div className="space-y-2"><label className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Workspace</label><select className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={workspaceId ?? ""} onChange={event => { setWorkspaceId(Number(event.target.value)); setProjectId(undefined); }}><option value="">Select a workspace</option>{spaces.data?.map(space => <option key={space.id} value={space.id}>{space.name} · {space.role}</option>)}</select></div><div className="space-y-2"><label className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Studio project</label><select className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={projectId ?? ""} onChange={event => setProjectId(Number(event.target.value))}><option value="">Choose a project</option>{projects.data?.map(project => <option key={project.id} value={project.id}>{project.name} · {project.status}</option>)}</select></div></CardContent></Card>

    {selectedProject ? <><section className="grid gap-5 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-[#d9ae72] bg-[#fff7e9]"><CardHeader><CardTitle>{selectedProject.name}</CardTitle><CardDescription>{selectedProject.description || "Studio project with an auditable workflow history."}</CardDescription></CardHeader><CardContent><p className="text-xs font-semibold uppercase tracking-[0.13em] text-[#c65e32]">Current stage</p><div className="mt-4 space-y-2">{stageOrder.map(stage => <button key={stage} disabled={transition.isPending || selectedProject.status === stage} onClick={() => workspaceId && transition.mutate({ workspaceId, projectId: selectedProject.id, toStatus: stage })} className={`flex w-full items-center justify-between rounded-xl border px-3 py-2 text-left text-sm capitalize transition ${selectedProject.status === stage ? "border-[#516f5e] bg-[#e4eee6] font-medium text-[#405c4c]" : "border-[#d9ae72]/50 bg-white/60 hover:bg-white"}`}><span>{stage.replace("_", " ")}</span>{selectedProject.status === stage ? <CheckCircle2 className="h-4 w-4" /> : <ArrowRight className="h-4 w-4 text-muted-foreground" />}</button>)}</div></CardContent></Card><Card className="border-border bg-white shadow-sm"><CardHeader><div className="flex items-start justify-between gap-4"><div><CardTitle>Project assets</CardTitle><CardDescription className="mt-1">A maximum of 5 MiB per file applies to this initial upload path.</CardDescription></div><label className="inline-flex h-9 cursor-pointer items-center rounded-lg bg-[#17202b] px-3 text-sm font-medium text-white transition hover:bg-[#2a3746]"><FileUp className="mr-1 h-4 w-4" />{upload.isPending ? "Uploading…" : "Upload asset"}<Input type="file" className="sr-only" onChange={onFile} disabled={upload.isPending} /></label></div></CardHeader><CardContent className="space-y-3">{uploadError ? <p className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive">{uploadError}</p> : null}{assets.isLoading ? <p className="text-sm text-muted-foreground">Loading project assets…</p> : null}{assets.data?.map(asset => <div key={asset.id} className="flex items-center justify-between rounded-xl border border-border bg-muted/30 p-3"><div><p className="text-sm font-medium">{asset.name}</p><p className="mt-1 text-xs text-muted-foreground">{asset.mediaType} · {(asset.sizeBytes / 1024).toFixed(1)} KB · {asset.status}</p></div><div className="flex items-center gap-2"><Button size="sm" variant="outline" disabled={downloadUrl.isPending} onClick={() => workspaceId && downloadUrl.mutate({ workspaceId, assetId: asset.id })}>Open</Button><span className="rounded-full bg-[#e4eee6] px-2.5 py-1 text-xs font-medium text-[#405c4c]">v1</span></div></div>)}{!assets.isLoading && assets.data?.length === 0 ? <div className="rounded-2xl border border-dashed border-border p-6 text-center"><FileUp className="mx-auto h-6 w-6 text-[#c65e32]" /><p className="mt-3 text-sm font-medium">No project assets yet.</p><p className="mt-1 text-xs text-muted-foreground">Upload the first approved working asset through this tenant-scoped flow.</p></div> : null}</CardContent></Card></section>
      <section className="grid gap-5 lg:grid-cols-2"><Card className="border-border bg-white"><CardHeader><CardTitle>Review and approval</CardTitle><CardDescription>Request decisions from shared workspace members and record the response against this project.</CardDescription></CardHeader><CardContent className="space-y-4"><div className="grid gap-2 sm:grid-cols-[1fr_0.75fr_auto]"><Input value={reviewNote} onChange={event => setReviewNote(event.target.value)} placeholder="What needs review?" /><select className="h-10 rounded-md border border-input bg-background px-3 text-sm" value={reviewerUserId ?? ""} onChange={event => setReviewerUserId(event.target.value ? Number(event.target.value) : undefined)}><option value="">Any eligible member</option>{members.data?.map(member => <option key={member.userId} value={member.userId}>{member.name || member.email || `User #${member.userId}`} · {member.role}</option>)}</select><Button disabled={!workspaceId || !projectId || requestReview.isPending} onClick={() => workspaceId && projectId && requestReview.mutate({ workspaceId, projectId, requestNote: reviewNote || undefined, reviewerUserId })} className="bg-[#516f5e] hover:bg-[#405c4c]"><Send className="mr-1 h-4 w-4" />Request</Button></div>{reviews.isLoading ? <p className="text-sm text-muted-foreground">Loading review records…</p> : null}{reviews.data?.map(review => <article key={review.id} className="rounded-xl border border-border p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium">{review.requestNote || "Review requested"}</p><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${review.status === "approved" ? "bg-[#e4eee6] text-[#405c4c]" : review.status === "changes_requested" ? "bg-[#fff0d7] text-[#a9522d]" : "bg-muted text-muted-foreground"}`}>{review.status.replace("_", " ")}</span></div><p className="mt-2 text-xs text-muted-foreground">{review.reviewerUserId ? `Assigned reviewer #${review.reviewerUserId}` : "Open to eligible workspace members"}</p>{review.status === "pending" ? <div className="mt-3 flex gap-2"><Button size="sm" variant="outline" disabled={respond.isPending} onClick={() => workspaceId && respond.mutate({ workspaceId, reviewId: review.id, status: "changes_requested" })}><ThumbsDown className="mr-1 h-3.5 w-3.5" />Changes</Button><Button size="sm" disabled={respond.isPending} onClick={() => workspaceId && respond.mutate({ workspaceId, reviewId: review.id, status: "approved" })} className="bg-[#516f5e] hover:bg-[#405c4c]"><CheckCircle2 className="mr-1 h-3.5 w-3.5" />Approve</Button></div> : <p className="mt-3 text-xs text-muted-foreground">Resolved {review.respondedAt ? new Date(review.respondedAt).toLocaleString() : ""}</p>}</article>)}{reviews.data?.length === 0 ? <p className="rounded-xl border border-dashed p-4 text-sm text-muted-foreground">No review requests have been created for this project.</p> : null}</CardContent></Card>
        <Card className="border-border bg-white"><CardHeader><CardTitle>Delivery preparation</CardTitle><CardDescription>Prepare a traceable delivery record. Publishing stays disabled until a provider adapter is configured and approved.</CardDescription></CardHeader><CardContent className="space-y-4"><div className="grid gap-2 sm:grid-cols-[0.7fr_1.3fr_auto]"><select className="h-10 rounded-md border border-input bg-background px-3 text-sm" value={destinationType} onChange={event => setDestinationType(event.target.value)}><option value="secure_link">Secure link</option><option value="client_portal">Client portal</option><option value="provider_handoff">Provider handoff</option></select><Input value={destinationRef} onChange={event => setDestinationRef(event.target.value)} placeholder="Optional destination reference" /><Button disabled={!workspaceId || !projectId || createDelivery.isPending} onClick={() => workspaceId && projectId && createDelivery.mutate({ workspaceId, projectId, destinationType, destinationRef: destinationRef || undefined })} className="bg-[#17202b] hover:bg-[#2a3746]">Create</Button></div>{deliveries.isLoading ? <p className="text-sm text-muted-foreground">Loading delivery records…</p> : null}{deliveries.data?.map(delivery => <article key={delivery.id} className="flex items-center justify-between gap-3 rounded-xl border border-border p-4"><div><p className="text-sm font-medium capitalize">{delivery.destinationType.replace("_", " ")}</p><p className="mt-1 text-xs text-muted-foreground">{delivery.destinationRef || "No external reference"} · {delivery.status}</p></div>{delivery.status === "draft" ? <Button size="sm" disabled={readyDelivery.isPending} onClick={() => workspaceId && readyDelivery.mutate({ workspaceId, deliveryId: delivery.id })} className="bg-[#516f5e] hover:bg-[#405c4c]"><PackageCheck className="mr-1 h-3.5 w-3.5" />Ready</Button> : <span className="rounded-full bg-[#e4eee6] px-2.5 py-1 text-xs font-medium text-[#405c4c]">{delivery.status}</span>}</article>)}{deliveries.data?.length === 0 ? <p className="rounded-xl border border-dashed p-4 text-sm text-muted-foreground">No delivery records exist yet.</p> : null}</CardContent></Card></section></> : <section className="grid gap-4 md:grid-cols-3">{[{ icon: Layers3, label: "Organize", copy: "Create a project in the Projects workspace before opening it in Studio." }, { icon: ScanSearch, label: "Review", copy: "Move the shared project through review and approval with a recorded transition." }, { icon: PackageCheck, label: "Deliver", copy: "Prepare approved assets for a traceable delivery or publishing adapter." }].map(stage => <Card key={stage.label} className="border-border bg-white shadow-sm"><CardHeader><stage.icon className="h-6 w-6 text-[#c65e32]" /><CardTitle className="mt-7 text-lg">{stage.label}</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-muted-foreground">{stage.copy}</p></CardContent></Card>)}</section>}
  </div></DashboardLayout>;
}
